# -----------------------
# Debian Functions & Aliases
# -----------------------

function udist () {
  sudo apt update &&
  sudo apt -u upgrade &&
  sudo apt --purge --assume-yes autoremove &&
  sudo apt clean &&
  sudo rm -rf /var/lib/apt/lists/* &&
  deborphan;
}
